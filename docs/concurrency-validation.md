# 每日统计和多路网页实测

2026-09-20，北京时间 12:33 起启用。当前部署：网页 HTTP GPT-6 Astra + Light thinking 优先，**网页并发上限 5**，超额请求交给原有 2 个 Codex 池工作进程。正常请求没有 Chrome、Xvfb 或 VNC。

## 真实并发

通过正式 `127.0.0.1:3467/v1` 入口，同时发起不同暗号的文本请求，混合 Responses SSE 和 Chat Completions JSON。逐条核对最终后端、模型、答案与完成事件；Responses 元数据返回 `gpt-6-astra-wm` / `min`，Chat 由同一工作进程执行模型校验。

| 网页上限 | 网页成功数 | 观测同时活跃网页数 | 单条完成用时 | 整组墙钟用时 |
| --- | ---: | ---: | --- | ---: |
| 3，强制网页 | 3/3 | 3 | 13.29～13.96 秒 | 14.03 秒 |
| 5，auto | 5/5 | 5 | 13.78～15.65 秒 | 15.70 秒 |

5 路已占用时再发起第 6 个 auto 请求，实际后端为 Codex GPT-6，回退原因为 `web_busy`，9.23 秒完成。五条网页响应均无回退，暗号均对应本请求，没有串流。调整 3→5 使用共享配置即时生效，没有重启网页服务。

## 网页资源

只统计网页服务内的 Bun/Python 进程，主代理与原生 Codex 另计；短文本、已预热环境，约 0.1 秒采样。

| 网页并发 | RSS 合计采样峰值 | PSS 合计采样峰值 | cgroup 内存采样峰值 |
| --- | ---: | ---: | ---: |
| 3 | 152.9 MiB | 107.1 MiB | 71.1 MiB |
| 5 | 230.6 MiB | 151.8 MiB | 114.3 MiB |

RSS、PSS 和 cgroup 对共享页/缓存的统计不同，不能混用。这是本机短请求测试结果，不是长输出、冷启动或持续负载的内存上限，也不承诺平台始终允许 5 路。

## 统计核对

- 第一组统计增加 3 次成功网页请求；第二组增加 5 次成功网页请求、1 次成功 Codex 请求和1次回退。总计 9 次客户端请求，未把回退重复计数。
- 8 次网页的可见文本估算累计输入 96、输出 31 Token；1 次 Codex 的上游实报为输入 9660、输出 8 Token。两类分开保存，输入口径不同，不能据此比较相同任务的真实消耗。
- 主服务无在途请求时重启，统计日期、开始记录时间、全部累计值以及文件摘要均保持一致；网页上限仍为 5。
- `/stats` 未认证或带 Origin 均返回 401；查询和修改设置不增加调用数。目录 0700、汇总文件 0600，只保存数字，不保存提示词、答案、请求标识或凭据。
- 启用前的内存计数没有补造为按天记录。上述数字包含本次测试请求；后续正常应用调用继续累计。

## 自动化检查

主代理构建及 **158 项测试全部通过**，包含跨北京时间零点、持久化重建、并发写入、回退计一次、实报/估算/未知分离、200 流式错误、取消、损坏文件保留、并发槽位、限流冷却竞态和动态设置。

Bun HTTP 工作进程与并发配置的 **4 项针对性测试全部通过**，覆盖进程退出、取消、超时、缺失解释器、共享配置热更新和无效配置回退。HTTP 桥接打包、Python 语法和 Git 空白检查通过。未改动的浏览器登录及 Python 上游协议沿用先前验收。

操作方式和计数边界见[每日统计与网页并发](daily-stats.md)。
