# 验证记录

2026-09-17，在现有 Linux/systemd 主机上完成以下检查：

- TypeScript 重新构建成功；上游 127 项单元测试通过。
- Python 启动器、安装器和管理命令语法检查通过。
- 无密钥请求与携带 Origin 的请求返回 401；合法本机 Bearer 请求健康检查返回 200。
- 独立工作目录中的真实 Responses 请求成功。
- 正式服务的真实 Chat Completions 请求及 SSE 流式请求成功。
- 后端环境变量注入正常，命令不输出凭据。
- systemd 服务 active/enabled，监听地址为 127.0.0.1；业务客户端继续连接原端口。
- Skill 元数据、文档引用、全局规则和自动发现链接检查通过。
- 准备提交的文件中未包含当前本机密钥、常见 token/private-key 格式、运行时凭据和本机迁移备份。

边界：本次使用现有已安装依赖迁移服务；全新机器上的安装流程尚未端到端验证。安装器已验证参数处理、语法及拒绝覆盖现有安装的行为。没有实测图像、外部工具执行、多账户切换或全部 Responses 字段。

这些结果只说明核对时本机配置可用，不保证不同账户、未来 Codex 版本或上游模型变更后行为不变。
